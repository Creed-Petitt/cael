echo "[TEST] Syntax (No Braces)";

let x = 10;
let result = "";

if (x > 5) result = "big";
if (result != "big") { echo "FAIL: Simple If"; exit(1); }

if (x < 5) result = "small"; else result = "huge";
if (result != "huge") { echo "FAIL: If-Else"; exit(1); }

let i = 0;
while (i < 3) i = i + 1;
if (i != 3) { echo "FAIL: While Loop"; exit(1); }

let sum = 0;
for (let j=0; j<5; j=j+1) sum = sum + j;
if (sum != 10) { echo "FAIL: For Loop"; exit(1); }

echo "PASS: Syntax Module";