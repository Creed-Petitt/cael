echo "--- Web Scraper Example ---";
let target = "http://example.com";
echo "Fetching: " + target;

let html = http_get(target);
let title = extract(html, "<title>", "</title>");

if (title == null) 
    echo "Could not find title. Note: Google uses uppercase <TITLE>.";
else 
    echo "Page Title: " + title;